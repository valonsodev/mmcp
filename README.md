Run `opencode` in this directory to search
https://opencode.ai/

Or look at the `opencode.json` file for configuration details.

-p : Number of pages to fetch per query (default: 5)
--no-descriptions : Do not include item descriptions in the output (less tokens but less accurate when titles are vague)